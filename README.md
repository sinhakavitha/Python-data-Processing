# Customer Data Preprocessing Assignment

##  Overview

This project focuses on cleaning and preparing raw customer data for machine learning. The dataset contains missing values, duplicate records, and categorical features that need encoding.

* **Min-Max Scaling**: Best when values need to be in a fixed range (0–1), especially for deep learning models.
* **Standardization**: Useful when data has outliers or when models assume normal distribution.
